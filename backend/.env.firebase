# Firebase environment configuration
OPENAI_API_KEY=your-openai-api-key-here
SECRET_KEY=your-secret-key-here
CORS_ORIGINS=https://ai-symptom-analyzer.web.app,https://ai-symptom-analyzer.firebaseapp.com
FIRESTORE_PROJECT_ID=ai-symptom-analyzer
ENVIRONMENT=production

# Optional: If using external database instead of Firestore
# DATABASE_URL=your-external-database-url

# Optional: Redis for caching (can use Firebase extensions)
# REDIS_URL=your-redis-url
